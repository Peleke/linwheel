# Database Configuration
# Local SQLite (default for development):
DATABASE_URL=file:./linwheel.db
# Turso (production):
# DATABASE_URL=libsql://your-db-name.turso.io
# DATABASE_AUTH_TOKEN=your-turso-auth-token

# LLM Configuration
# Provider priority: Claude (if ANTHROPIC_API_KEY set) > OpenAI
# Force provider: LLM_PROVIDER=openai or LLM_PROVIDER=claude
ANTHROPIC_API_KEY=your-anthropic-api-key-here
# ANTHROPIC_MODEL=claude-sonnet-4-20250514

# OpenAI API Key (fallback for content generation, required for image generation)
OPENAI_API_KEY=your-openai-api-key-here
# OPENAI_MODEL=gpt-4o-mini

# Text-to-Image Configuration
# Provider: openai | comfyui | fal (default: openai)
T2I_PROVIDER=openai
# OpenAI Image Model: gpt-image-1 | gpt-image-1.5 (default: gpt-image-1)
OPENAI_IMAGE_MODEL=gpt-image-1

# ComfyUI Configuration (for local generation)
# COMFYUI_SERVER_URL=http://localhost:8188

# FAL.ai Configuration (for cloud generation)
# FAL_KEY or FAL_API_KEY - either works
# FAL_KEY=your-fal-api-key-here
# FAL_MODEL: flux-dev | flux-pro | recraft-v3 (default: flux-dev)
# FAL_MODEL=flux-dev

# Supabase Configuration (for storage + future auth)
# Required for image storage on Vercel (carousel images)
# Create a "images" bucket in Supabase Storage dashboard
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
